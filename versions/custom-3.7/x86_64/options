export RELEASE="v3.7"
export MIRROR="http://dl-cdn.alpinelinux.org/alpine"
export PACKAGES="alpine-baselayout,busybox,alpine-keys,apk-tools,libc-utils"
export BUILD_OPTIONS=(-b -s -t UTC -r $RELEASE -m $MIRROR -p $PACKAGES -d)
export TAGS=(custalpine:3.7 custalpine:latest)
